# name: 05 - 1 - Event Filters and Activity Types
# on: 
#   pull_request:
#     types:
#       - opened
#       - synchronize
#     branches:
#       - main

# jobs:
#   job-1:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Running whenever a PR is opened or synchronized AND base branch is main"


name: 05 - 1 - Event Filters and Activity Types

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main]

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running whenever a PR is opened or synchronized AND base branch is mainn"
